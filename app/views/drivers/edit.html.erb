<h1>Editing <%= @driver.name %> </h1>

<%= form_with model: @driver do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :vin %>
  <%= f.text_field :vin %>
  <%= f.submit "Update" %>
<% end %>

