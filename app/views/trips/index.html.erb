<h1>Trips#index</h1>
<p>Cash me in app/views/trips/index.html.erb</p>

<ul>
  <% @trips.each do |trip| %>
    <li>
      <%= "ID: #{trip.id}"%>
      <%= "Driver: #{trip.driver.name}" if trip.driver%>
    </li>
  <% end %>
</ul>
