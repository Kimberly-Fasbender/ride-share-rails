<%= form_with model: @trip, url: :trip_path(@trip.id), method: :patch, form_class: "rate trip form" do |f| %>

  <%= f.label :rate_trip %>
  <%= f.number_field :rating, in: 1..5, step: 1  %>

  <%= f.submit "Submit", class: "rate trip form button"%>

<%end%>